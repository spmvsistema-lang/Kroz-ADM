import { z } from "zod";
import type { User } from './data';

// This file is intended for schema definitions and static data,
// not for fetching dynamic data. The user list is now fetched
// directly in the page component based on the logged-in client.
